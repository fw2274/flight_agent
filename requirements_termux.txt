# Flight Agent Dependencies for Termux (ARM64 Android)
# Install in Termux after: pkg install python rust clang portaudio

# Core dependencies - lighter versions for mobile
google-genai>=1.0.0
python-dotenv>=1.0.0
asyncio>=3.4.3

# HTTP and API clients
requests>=2.31.0
httpx>=0.24.0

# Pydantic for data validation
pydantic>=2.0.0

# Audio support (Termux has native portaudio)
# PyAudio may not work well in Termux, use simpler alternatives
sounddevice>=0.4.6

# JSON and data handling
typing-extensions>=4.5.0

# Note: LangGraph and google-adk may have dependency issues in Termux
# We'll create a simplified version that works with basic Google AI SDK
